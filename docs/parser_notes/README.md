# Parser Notes

Use this directory for parser templates, known HTML structures, template changes, extraction warnings, and source-specific parse decisions.

Each parser note should record:

- source and template period;
- example raw paths or URLs;
- fields recoverable;
- comment availability;
- timestamp/order availability;
- known failure modes;
- parser version.
