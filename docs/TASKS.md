# Tasks

Root `TASKS.md` is the canonical task tracker for this repository.

This file exists for compatibility with the research governance overlay, Claude Code commands, Cursor rules, and Codex skills that expect `docs/TASKS.md`.

When updating tasks, update root `TASKS.md`.
